/*
Description:

Now you have to write a function that takes an argument and returns the square of it.
Fundamentals
*/
const square = (n) => n ** 2;
// const square = (n) => n * n
// const square = (n) => Math.pow(n, 2)
// const square = (n) => Math.sqrt(n ** 4)

console.log(square(3)); //9
console.log(square(5));
